p = float(input('Enter the principal amount: '))#receive variables from the userfor principal amount
r = float(input('Enter the rate of interest: '))#receive variables from the userfor rate of interest
t = int(input('Enter the time(in years): '))#receive variables from the userfor time
s = (p*r*t)/100#receive variables from the user and apply the formula
print(s)#print the result


